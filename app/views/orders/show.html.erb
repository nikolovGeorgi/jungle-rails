<section class="orders-show">

  <header class="page-header">
  <% if current_user %>
    <h2>Thank you for your purchase <%= @current_user.first_name %>!</h2>
  <% else %>
    <h2>Thank you for your purchase dear Guest!</h2>
  <% end %>
</header>

  <div class="panel panel-default">
    <table class="table table-bordered table-responsive">

      <thead>
        <tr>
          <th colspan="2">Product</th>
          <th>Unit Price</th>
          <th>Quantity</th>
          <th>Price</th>
        </tr>
      </thead>

      <tbody>
        <% @order.line_items.each do |line_item| %>
        <%= render 'item', line_item: line_item %>
        <% end %>
      </tbody>

      <tfoot>
        <tr>
          <th colspan="4">
            TOTAL:
          </th>
          <th>
            <%= number_to_currency(@order.total_cents/100.00, :precision => 2) %>
          </th>
        </tr>

      </tfoot>

    </table>
  </div>

  <%= link_to "Continue Shopping", root_path, class: 'btn btn-lg btn-primary' %>

</section>
